package lemonway;
 
class CancelUrl extends controller.Controller
{
 
  public function new() 
  {
    super();
    
  }
  
  
  //@tpl("amapadmin/default.mtt")
  function doDefault() {
    //var returnData = haxe.Json.parse(app.params.get("data"));
    sys.db.Manager manager = new sys.db.Manager();
    
        throw Ok('/', app.params.toString();
  }
}